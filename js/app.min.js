let footer = document.querySelector('.footer'); let logo = document.querySelector('.wrapper_logo'); let button_end = document.querySelector('.button_end'); let toggle_text = document.querySelector('.toggle_text'); let zSpecing = -1400, lastPos = zSpecing / 5, $frames = document.querySelectorAll('.frame'), frames = Array.from($frames), zVals = []
function update() {
    let top = document.documentElement.scrollTop.toFixed(); delta = lastPos - top; lastPos = top; top > 1 ? footer.classList.add('none') & logo.classList.add('none_logo') : footer.classList.remove('none') & logo.classList.remove('none_logo'); top > 500 && toggle_text.classList.add('done2'); frames.forEach(function (n, i) {
        zVals.push((i * zSpecing) + zSpecing)
        zVals[i] += delta * -5; let frame = frames[i], transform = `translateZ(${zVals[i]}px)`
        opacity = zVals[i] < Math.abs(zSpecing) / 1.8 ? 1 : 0
        frame.setAttribute('style', `transform: ${transform};opacity: ${opacity}`)
    })
}

button_end.addEventListener('click', () => window.scrollTo({ top: -10, behavior: 'smooth', }))
let preloader = document.querySelector('.preloader'); let body = document.querySelector('body'); window.onload = () => {
    setTimeout(() => { !preloader.classList.contains('done') && preloader.classList.add('done') }, 500), setTimeout(() => { preloader.classList.contains('done') && preloader.classList.add('done2'), body.classList.add('plus_overflow') }, 1000); let E = !1; window.addEventListener('scroll', () => {
        if (E) { return }
        update(); E = !0; setTimeout(() => { E = !1 }, 12)
    }); window.scrollTo({ top: 1 })
}

//  document.addEventListener("DOMContentLoaded", () => {});
